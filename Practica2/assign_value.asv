function [x,domains] = assign_value(x,domains, v, value)
%UNTITLED Summary of this function goes here
%   Detailed explanation goes here
x(v)=value;
ad = find(G(var,:) ==1);
AdyacentesNoColoreadas =(x(CiudadesAdyacentes)==0);
aux = domains(AdyacentesNoColoreadas,value)==1;
domains(aux,value) =0;
end


function displaySatelites(satelites,representatives)
%Shows grafically where Satelites and Representatives are located.
[N,~]=size(satelites);
indexes = (1:N);
notRepresentatives = setdiff(indexes,representatives);
plot(satelites(notRepresentatives,
end

